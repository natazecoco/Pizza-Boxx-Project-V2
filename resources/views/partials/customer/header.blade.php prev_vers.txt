<header x-data="{ mobileMenuOpen: false, scrolled: false }" 
        @scroll.window="scrolled = (window.pageYOffset > 20)"
        :class="{ 'shadow-xl bg-white/100 border-b-brand-red/20': scrolled, 'shadow-sm bg-white/90': !scrolled }"
        class="fixed top-0 left-0 w-full z-50 transition-all duration-300 backdrop-blur-md border-b border-gray-100">
    
    <nav class="container mx-auto px-4 lg:px-12 py-3 flex items-center justify-between">
        
        {{-- LOGO --}}
        <div class="flex-1 flex items-center">
            <a href="{{ route('home') }}" class="flex items-center gap-2 group transition-all duration-300">
                <div class="relative">
                    <img src="{{ asset('images/pizza-boxx-logo.png') }}"
                         alt="Logo"
                         class="h-12 w-12 object-contain drop-shadow-sm transition-transform duration-500 group-hover:rotate-[360deg]">
                    <div class="absolute -inset-1 bg-red-100 rounded-full scale-0 group-hover:scale-100 transition-transform duration-300 -z-10"></div>
                </div>
            </a>
        </div>

        {{-- DESKTOP NAV --}}
        <div class="hidden lg:flex items-center justify-center gap-8">
            @php
                $navLinks = [
                    ['route' => 'home', 'label' => 'HOME'],
                    ['route' => 'menu.index', 'label' => 'MENU'],
                    ['route' => 'about', 'label' => 'ABOUT'],
                    ['route' => 'contact', 'label' => 'CONTACT'],
                ];
            @endphp

            @foreach($navLinks as $link)
                <a href="{{ route($link['route']) }}"
                   class="group relative py-2 text-[14px] font-bold tracking-[0.2em] transition-colors duration-300
                          {{ request()->routeIs($link['route']) ? 'text-brand-red' : 'text-gray-500 hover:text-brand-red' }}">
                    {{ $link['label'] }}
                    <span class="absolute bottom-0 left-0 h-0.5 bg-brand-red transition-all duration-300
                                 {{ request()->routeIs($link['route']) ? 'w-full' : 'w-0 group-hover:w-full' }}"></span>
                </a>
            @endforeach
        </div>

        {{-- RIGHT SECTION (Cart & User) --}}
        <div class="flex-1 flex items-center justify-end gap-2 sm:gap-4">
            
            {{-- CART --}}
            <a href="{{ route('cart.index') }}" class="relative p-2.5 rounded-2xl text-gray-700 hover:bg-red-50 hover:text-brand-red transition-all group">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                </svg>
                @php $cartCount = session('cart') ? count(session('cart')) : 0; @endphp
                @if($cartCount > 0)
                    <span class="absolute top-1.5 right-1.5 bg-brand-red text-white text-[10px] font-black rounded-full w-5 h-5 flex items-center justify-center border-2 border-white shadow-sm">
                        {{ $cartCount }}
                    </span>
                @endif
            </a>

            {{-- USER (Desktop Only) --}}
            <div class="hidden lg:block">
                @if(Auth::check() && !Auth::user()->hasAnyRole(['admin', 'employee']))
                    <div class="relative" x-data="{ open: false }" @click.away="open = false">
                        <button @click="open = !open" 
                                class="flex items-center gap-2 px-3 py-2 rounded-2xl hover:bg-gray-50 transition-all border border-transparent hover:border-gray-100">
                            <div class="w-8 h-8 rounded-xl bg-gray-900 flex items-center justify-center text-white text-xs font-black shadow-lg shadow-gray-200">
                                {{ strtoupper(substr(Auth::user()->name, 0, 1)) }}
                            </div>
                            <span class="hidden sm:block text-xs font-black text-gray-900 uppercase tracking-widest italic">
                                {{ Str::of(Auth::user()->name)->explode(' ')->first() }}
                            </span>
                            <i class="fas fa-chevron-down text-[10px] text-gray-400 transition-transform duration-300" :class="open ? 'rotate-180' : ''"></i>
                        </button>

                        {{-- DROPDOWN MENU CONTENT --}}
                        <div x-show="open"
                            x-transition:enter="transition ease-out duration-200"
                            x-transition:enter-start="opacity-0 translate-y-2"
                            x-transition:enter-end="opacity-100 translate-y-0"
                            x-transition:leave="transition ease-in duration-150"
                            x-transition:leave-start="opacity-100 translate-y-0"
                            x-transition:leave-end="opacity-0 translate-y-2"
                            class="absolute right-0 mt-3 w-64 bg-white rounded-[2rem] shadow-2xl py-4 z-50 border border-gray-100 overflow-hidden"
                            style="display: none;">
                            
                            <div class="px-6 py-4 mb-2 border-b border-gray-50">
                                <p class="text-[10px] font-black text-gray-400 uppercase tracking-widest leading-none mb-2">Selamat Datang,</p>
                                <p class="text-sm font-black text-gray-900 truncate uppercase italic tracking-tighter">{{ Auth::user()->name }}</p>
                            </div>

                            <div class="px-3 space-y-1">
                                <a href="{{ route('user.profile') }}" class="flex items-center gap-3 px-4 py-3 rounded-xl text-gray-600 hover:bg-red-50 hover:text-brand-red transition-all group">
                                    <div class="w-8 h-8 rounded-lg bg-gray-50 group-hover:bg-white flex items-center justify-center transition-colors">
                                        <i class="fas fa-user-circle text-sm"></i>
                                    </div>
                                    <span class="text-[11px] font-black uppercase tracking-widest">Akun Saya</span>
                                </a>

                                <a href="{{ route('user.dashboard') }}" class="flex items-center gap-3 px-4 py-3 rounded-xl text-gray-600 hover:bg-red-50 hover:text-brand-red transition-all group">
                                    <div class="w-8 h-8 rounded-lg bg-gray-50 group-hover:bg-white flex items-center justify-center transition-colors">
                                        <i class="fas fa-box-open text-sm"></i>
                                    </div>
                                    <span class="text-[11px] font-black uppercase tracking-widest">Orderan Saya</span>
                                </a>
                            </div>

                            <div class="mt-4 px-3 border-t border-gray-50 pt-4">
                                <form action="{{ route('logout') }}" method="POST">
                                    @csrf
                                    <button type="submit" class="w-full flex items-center gap-3 px-4 py-3 rounded-xl text-brand-red hover:bg-brand-red hover:text-white transition-all group shadow-sm hover:shadow-red-200">
                                        <i class="fas fa-sign-out-alt text-sm"></i>
                                        <span class="text-[11px] font-black uppercase tracking-widest">Keluar Sekarang</span>
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                @else
                    <button @click.prevent="$dispatch('open-auth-modal', { form: 'login' })"
                            class="h-11 px-8 rounded-2xl bg-brand-red text-white text-xs font-black uppercase tracking-[0.2em] shadow-xl shadow-red-200 hover:bg-gray-900 transition-all active:scale-95">
                        Masuk
                    </button>
                @endif
            </div>

            {{-- HAMBURGER BUTTON (Mobile Only) --}}
            <button @click="mobileMenuOpen = !mobileMenuOpen" class="lg:hidden p-2 text-gray-900 transition-transform active:scale-90">
                <i class="fas" :class="mobileMenuOpen ? 'fa-times text-2xl' : 'fa-bars text-xl'"></i>
            </button>
        </div>
    </nav>

    {{-- MOBILE MENU OVERLAY --}}
    <div x-show="mobileMenuOpen" 
         x-transition:enter="transition ease-out duration-300"
         x-transition:enter-start="opacity-0 translate-y-[-10px]"
         x-transition:enter-end="opacity-100 translate-y-0"
         x-transition:leave="transition ease-in duration-200"
         x-transition:leave-start="opacity-100 translate-y-0"
         x-transition:leave-end="opacity-0 translate-y-[-10px]"
         class="lg:hidden fixed inset-x-0 top-[72px] bg-white shadow-2xl border-t border-gray-100 overflow-hidden z-40" 
         style="display: none;">
        
        <div class="p-6 space-y-4 bg-gray-50/50">
            @foreach($navLinks as $link)
                <a href="{{ route($link['route']) }}" 
                   class="block px-6 py-4 rounded-2xl font-black text-sm tracking-widest uppercase transition-all
                          {{ request()->routeIs($link['route']) ? 'bg-brand-red text-white shadow-lg shadow-red-200' : 'bg-white text-gray-500 border border-gray-100' }}">
                    {{ $link['label'] }}
                </a>
            @endforeach

            <hr class="border-gray-200 my-4 border-dashed">

            @if(Auth::check())
                {{-- Info Akun --}}
                <div class="flex items-center gap-4 p-4 bg-white rounded-3xl border border-gray-100 mb-4">
                    <div class="w-12 h-12 rounded-2xl bg-gray-900 flex items-center justify-center text-white font-black shadow-lg">
                        {{ strtoupper(substr(Auth::user()->name, 0, 1)) }}
                    </div>
                    <div>
                        <p class="text-[9px] font-black text-gray-400 uppercase tracking-widest">Selamat Datang,</p>
                        <p class="text-sm font-black text-gray-900 uppercase italic">{{ Auth::user()->name }}</p>
                    </div>
                </div>

                {{-- Link Navigasi User (Dua Kotak Berjajar) --}}
                <div class="grid grid-cols-2 gap-3 mb-4">
                    <a href="{{ route('user.profile') }}" 
                    class="flex flex-col items-center justify-center p-5 bg-white rounded-[2rem] border border-gray-100 text-gray-600 active:bg-red-50 active:text-brand-red transition-all shadow-sm">
                        <i class="fas fa-user-circle text-xl mb-2"></i>
                        <span class="text-[9px] font-black uppercase tracking-[0.2em]">Profil</span>
                    </a>

                    <a href="{{ route('user.dashboard') }}" 
                    class="flex flex-col items-center justify-center p-5 bg-white rounded-[2rem] border border-gray-100 text-gray-600 active:bg-red-50 active:text-brand-red transition-all shadow-sm">
                        <i class="fas fa-box-open text-xl mb-2"></i>
                        <span class="text-[9px] font-black uppercase tracking-[0.2em]">Orderan</span>
                    </a>
                </div>

                {{-- Tombol Logout --}}
                <form action="{{ route('logout') }}" method="POST">
                    @csrf
                    <button type="submit" class="w-full p-5 rounded-[2rem] bg-gray-100 text-brand-red font-black text-[10px] uppercase tracking-[0.3em] flex items-center justify-center gap-3 active:bg-brand-red active:text-white transition-all">
                        <i class="fas fa-sign-out-alt"></i>
                        <span>Keluar Sekarang</span>
                    </button>
                </form>
            @else
                {{-- Bagian tombol masuk untuk tamu tetap seperti sebelumnya --}}
                <button @click="mobileMenuOpen = false; $dispatch('open-auth-modal', { form: 'login' })" 
                        class="w-full p-6 rounded-[2.5rem] bg-gray-900 text-white font-black text-xs uppercase tracking-[0.2em] shadow-2xl">
                    MASUK KE AKUN
                </button>
            @endif
        </div>
    </div>
</header>